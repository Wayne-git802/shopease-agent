# Changelog — 2026-05-29

## 1. 前端聊天 UI 重构

**问题**：
- 硬编码回复用 `<b>` 标签，被 `formatMarkdown()` 转义为纯文本
- 消息气泡 `max-width: 75%` 双层压缩，短文本被拆成多行
- 思考指示器白框撑满全宽

**修复**：
- `workspace.html`: `<b>...</b>` → `**...**` markdown
- `ai.css`: 重写 Chat Conversation UI 区块
  - 新增 `.chat-bubble-wrapper`（`flex:1` + `min-width:0`），替代 inline `maxWidth`
  - 用户气泡：蓝色渐变 + 微阴影
  - 助手气泡：白底 + 浅灰边框 + `align-items:flex-start`（修复思考框宽度）
  - 输入框：20px pill 圆角 + 圆形渐变发送按钮
- 删除硬编码的"解答平台问题/优惠券"（未实现），改为实际能力列表

**文件**：
- `frontend/templates/ai/workspace.html`
- `frontend/static/css/ai.css`

---

## 2. WorkflowAffinityRouter — 上下文亲和路由

**问题**：
- 下单后说"不要了" → PurchaseAgent 返回空壳"已取消"，订单未真实取消
- orchestrator 有两坨 ad-hoc 检查（OrderWorkflow check + PurchaseAgent shortcut），散落且不完整

**设计**：
- 新增 `ConversationActionContext` + `ActionBlock` dataclass
- `build_action_context()`: 扫最近 3 条 assistant 消息的 blocks（TTL 300s）
- `route_by_affinity()`: P0 strong intent 打断 → P1 block 匹配 → P2 fall through
- 亲和路由表：`order_created_card→order`, `confirm_dialog→purchase`, `cart_card→cart`
- 替换 orchestrator 两坨 ad-hoc 为统一入口

**时序**：00:09–00:17

**文件**：
- `backend/agents/graph/orchestrator.py`（+120 行新增，-30 行删除）

---

## 3. HuggingFace 嵌入模型下载

**问题**：
- RAG 搜索触发 `embedder.py` 加载 `paraphrase-multilingual-MiniLM-L12-v2`
- 代码设 `local_files_only=True`（因 HF 国内被墙），但模型从未下载
- 报错：`We couldn't connect to 'https://huggingface.co'`

**修复**：
- 从 hf-mirror.com 镜像下载模型至 `~/.cache/huggingface/hub/`（~620MB）

**时序**：00:09–00:10

---

## 4. QueryGrounder — 查询落地层

**问题**：
- "推荐礼物"直接查数据库，只匹配商品名含"礼物"
- "有什么好看的"无品类锚点，搜索结果为空

**设计**：
- 新增 `agents/graph/grounding.py`
- `ProductLexicon`: 三类标签（category / abstract_goal / descriptor）
- `ground(query)`: 纯词法提取 slots（category, abstract_goal, descriptor, budget, recipient, occasion, usage）
- `plan(slots, depth)`: 三态决策 `READY` | `NEEDS_REFINEMENT` | `NEEDS_CATEGORY`
- `GroundingMemory`: 约束积累（非 query string merge）
- `build_clarify_reply()`: 确定性模板回复
- `MAX_CLARIFY_DEPTH = 2`: 防止无限 clarify loop

**路由规则**：
| 查询 | 状态 | 行为 |
|------|------|------|
| "推荐耳机" | READY | 直接搜索 |
| "推荐礼物" | NEEDS_CATEGORY | clarify category |
| "送给女朋友的礼物" | NEEDS_REFINEMENT | 搜索（有辅助约束） |
| "有什么好看的" | NEEDS_CATEGORY | clarify category |
| "第一个看看" | 跳过（有 card + 无新锚点） | DisplayContext |

**时序**：00:20–01:25

**文件**：
- `backend/agents/graph/grounding.py`（新建，~220 行）
- `backend/agents/graph/orchestrator.py`（+35 行插入）

---

## 5. commerce_intent 裸品类词置信度修复

**问题**：
- "耳机" 仅匹配 PRODUCT_NOUNS，无 signal keyword → 置信度 0.0 → response_policy 路由到 LLM chat
- 导致 bare product noun 不进 graph search，无 product_card 产出

**修复**：
- `commerce_intent.py`: `all(scores == 0.0) and has_product_noun` → 返回 confidence=0.35

**文件**：
- `backend/agents/graph/commerce_intent.py`（+3 行）

---

## 6. Grounder 跳过逻辑迭代

**问题 v1**（词表方案）：
- `_CONTEXT_REF_PATTERNS` 硬编码"第一/那个/看看"，"耳机"（2字）被误判为 context ref

**问题 v2**（`_has_strong_intent` 方案）：
- "第一个看看"中"看看"在 commerce 关键词里 → `_has_strong_intent=True` → 跳过失败

**最终方案 v3**：
- `ground(query)` 提取品类锚点 → `has_new_anchor = bool(category)`
- `has_cards AND NOT has_new_anchor` → 跳过 grounder
- 不依赖任何词表或 `_has_strong_intent`

**文件**：
- `backend/agents/graph/grounding.py`（删 `_CONTEXT_REF_PATTERNS` + `is_context_reference`）
- `backend/agents/graph/orchestrator.py`（改 skip 逻辑）

---

## 剩余问题

### P1: 直接输入商品名，Agent 不展示 card
- **现象**：用户输入完整商品名（如 "Soundmagic PL30"）→ LLM chat 回复，无 product_card + 操作选项
- **预期**：识别为已知商品 → 展示 product_card + 询问"加入购物车 / 立即购买 / 查看详情"
- **可能根因**：commerce_intent 把完整商品名当 chat / 无 signal match → 不进 graph search

### P2: 数量 + 序号指代理解失败
- **现象**："买3个第二个" → "无法确定你指的是哪个订单"
- **预期**：解析 `quantity=3, index=2` → 找到 display context 中第 2 个商品 → 加购物车或下单
- **可能根因**：DisplayContext / CartAgent 未处理复合指代（序号 + 数量）

---

## 架构演进总结

```
本次新增的层：
  WorkflowAffinityRouter  (P0: block-driven)
  QueryGrounder            (L1.5: executability gate)
  GroundingMemory          (constraint accumulation)

当前完整路由链路：
  query
    ↓
  WorkflowAffinityRouter   — 最近 assistant block → 亲和路由
    ↓
  Dialogue context merge   — follows-up merge
    ↓
  L0 state_router          — 大类意图
    ↓
  L1 commerce_intent       — commerce 子意图
    ↓
  L1.5 QueryGrounder       — 可执行性门控（READY / clarify / skip）
    ↓
  L2 FinalDecision         — 单一真理源
    ↓
  Agent dispatch           — order / purchase / cart / commerce
```

## 改动文件清单

| 文件 | 类型 | 行数 |
|------|------|------|
| `frontend/templates/ai/workspace.html` | 修改 | ~20 |
| `frontend/static/css/ai.css` | 修改 | ~80 |
| `backend/agents/graph/orchestrator.py` | 修改 | +155 -30 |
| `backend/agents/graph/grounding.py` | 新建 | ~220 |
| `backend/agents/graph/commerce_intent.py` | 修改 | +3 |

---

# Changelog — 2026-06-08/09

## 1. 架构清理 — 共享词表 + 死代码删除 + ops 节点移除

**问题**：
- `PRODUCT_NOUNS` / `ProductLexicon` / `_KEYWORDS` 三处重复维护相同的中文产品词表
- `entry_router.py` 内含 LLM fallback 死代码 (~80行：`_llm_classify`, `_build_llm_prompt`, `_parse_llm_response`, `SYSTEM_PROMPT`)
- `ops_node` 49 行展示性代码，无业务价值

**修复**：
- 新建 `agents/graph/lexicon.py` — 统一 `PRODUCT_NOUNS(65)`, `CATEGORY_LEXICON(92)`, `COMMERCE_SIGNAL_WORDS(53)`
- `commerce_intent.py`, `grounding.py`, `state_router.py` 改为从 lexicon 导入
- `entry_router.py` 删 LLM fallback 全部死代码（safe degrade 替代）
- `graph_builder.py` 移除 ops 节点及所有引用，图从 9 节点 → 8 节点
- 删除 `ops_node.py`

**附带**：新建 `tests/test_entry_router.py` (23 tests) + `tests/test_merge_node.py` (29 tests)，全部通过

**commit**：`cf3b16a`

**文件**：
| 文件 | 类型 | 行数 |
|------|------|------|
| `backend/agents/graph/lexicon.py` | 新建 | ~100 |
| `backend/agents/graph/commerce_intent.py` | 修改 | -20 +3 |
| `backend/agents/graph/grounding.py` | 修改 | -42 +3 |
| `backend/agents/graph/state_router.py` | 修改 | -10 +4 |
| `backend/agents/graph/nodes/entry_router.py` | 修改 | -128 |
| `backend/agents/graph/nodes/ops_node.py` | 删除 | -49 |
| `backend/agents/graph/graph_builder.py` | 修改 | -6 |
| `backend/agents/graph/trace.py` | 修改 | -2 |
| `backend/tests/test_entry_router.py` | 新建 | ~230 |
| `backend/tests/test_merge_node.py` | 新建 | ~300 |
| `README.md` | 修改 | 8→7 节点 |

---

## 2. Sprint 1 — 正确性 & 稳定性

### 2a. PurchaseAgent 状态机 + 确认门控

**问题**：
- PurchaseAgent 无状态机 — "确认"两字直接下单，不检查状态
- 无确认令牌 — 无 TTL、无 token 绑定、无防重复
- `_resolve_product_id` 未利用 DisplayContext

**新增**：
- `purchase/state_machine.py` — PurchaseStep (IDLE→VIEWING→CONFIRMING→PURCHASED) + transitions + PurchaseSessionState
- `purchase/confirmation_gate.py` — sha256 快照哈希、3min TTL、防重复消费、价格/库存变更检测

### 2b. CartWorkflow + PurchaseWorkflow Model

**新增**：
- `agents/models.py` — `CartWorkflow` (7 fields) + `PurchaseWorkflow` (11 fields)
- `agents/migrations/0012_cart_purchase_workflow.py`

### 2c. CartAgent 持久化

**问题**：`_store: dict[str, CartSessionState] = {}` 进程重启全部丢失

**修复**：`_load_state` / `_save_state` 改为 CartWorkflow DB 读写，接口不变

**commit**：`28c5f29`

**文件**：
| 文件 | 类型 | 行数 |
|------|------|------|
| `backend/agents/purchase/state_machine.py` | 新建 | ~90 |
| `backend/agents/purchase/confirmation_gate.py` | 新建 | ~110 |
| `backend/agents/models.py` | 修改 | +55 |
| `backend/agents/migrations/0012_cart_purchase_workflow.py` | 新建 | ~40 |
| `backend/agents/cart/agent.py` | 修改 | +30 -15 |

---

## 3. Sprint 2 — 整机交付

### 3a. ReferenceResolver — 统一产品引用解析

**问题**：PurchaseAgent 和 CartAgent 各自拼接 DisplayContext + blocks + focused_item，逻辑重复且不一致

**新增**：`agents/graph/reference_resolver.py`
- `resolve()` — 三级解析：DisplayContext(0.95) → focused_item(0.85) → block(0.70)
- 返回 `ResolvedReference(source, product_id, confidence, name, price)`

### 3b. PurchaseAgent 完整重写

**问题**：原 agent.py 195 行，无状态机、无持久化、无确认门控

**重写**：
- `purchase/agent.py` — 9-step pipeline: auth→load_state→parse→resolve_ref→validate_transition→get_product→generate_token→update_state→confirm
- `purchase/agent.py` — `handle_confirm()` 确认流程：validate_token→consume→create_order→transition
- `purchase/workflow_store.py` — PurchaseWorkflow CRUD (load/save/delete)
- `purchase/repository.py` — DB 操作 (get_product, create_order, get_cart_items)
- `purchase/response.py` — 结构化响应 (build_confirm/build_success/build_error/build_decline)
- `orchestrator.py` — PurchaseAgent 路由增加 CONFIRMING 状态检测 + display_id 传递

### 3c. Trace Dashboard

**新增**：`frontend/templates/ai/trace_dashboard.html` (771行)
- 路由分布饼图 (Chart.js)
- 平均延迟折线图
- 最近 20 条 session trace 表格（置信度彩色徽章 + 延迟可视化条）
- 暗色主题，响应式布局，mock 数据 fallback

**commit**：`25cac5a`

**文件**：
| 文件 | 类型 | 行数 |
|------|------|------|
| `backend/agents/graph/reference_resolver.py` | 新建 | ~220 |
| `backend/agents/purchase/agent.py` | 重写 | 153 |
| `backend/agents/purchase/workflow_store.py` | 新建 | ~60 |
| `backend/agents/purchase/repository.py` | 新建 | ~90 |
| `backend/agents/purchase/response.py` | 新建 | ~70 |
| `backend/agents/purchase/__init__.py` | 新建 | 3 |
| `backend/agents/graph/orchestrator.py` | 修改 | +10 -3 |
| `frontend/templates/ai/trace_dashboard.html` | 新建 | 771 |

---

## 4. 架构演进总结

```
本次改动前后对比：

  改动前                                    改动后

  PurchaseAgent: ❌ 纯函数                  ✅ 状态机+确认门+DB持久化
  CartAgent:     ⚠️ 内存 dict               ✅ DB持久化
  entry_router:  ⚠️ LLM死代码残留            ✅ 干净
  ops_node:      ⚠️ 展示性代码               ✅ 已移除
  词表:          ❌ 三处重复                  ✅ 单文件共享
  测试:          ❌ 0 tests                  ✅ 52 tests
  可视化:        ❌ 无                       ✅ Trace Dashboard

  4 个 Agent 最终状态:
    OrderAgent       ✅ 95%  状态机+确认门+工作流恢复
    PurchaseAgent    ✅ 对标 状态机+确认门+DB持久化 (刚重构)
    CartAgent        ✅ DB持久化
    Commerce Graph   ✅ 85%  混合检索+RRF+策略融合
```

## 改动文件总清单

| 文件 | 类型 | 
|------|------|
| `backend/agents/graph/lexicon.py` | 新建 |
| `backend/agents/graph/reference_resolver.py` | 新建 |
| `backend/agents/graph/orchestrator.py` | 修改 |
| `backend/agents/graph/commerce_intent.py` | 修改 |
| `backend/agents/graph/grounding.py` | 修改 |
| `backend/agents/graph/state_router.py` | 修改 |
| `backend/agents/graph/graph_builder.py` | 修改 |
| `backend/agents/graph/trace.py` | 修改 |
| `backend/agents/graph/nodes/entry_router.py` | 修改 |
| `backend/agents/graph/nodes/ops_node.py` | 删除 |
| `backend/agents/models.py` | 修改 |
| `backend/agents/migrations/0012_cart_purchase_workflow.py` | 新建 |
| `backend/agents/cart/agent.py` | 修改 |
| `backend/agents/purchase/state_machine.py` | 新建 |
| `backend/agents/purchase/confirmation_gate.py` | 新建 |
| `backend/agents/purchase/agent.py` | 重写 |
| `backend/agents/purchase/workflow_store.py` | 新建 |
| `backend/agents/purchase/repository.py` | 新建 |
| `backend/agents/purchase/response.py` | 新建 |
| `backend/agents/purchase/__init__.py` | 新建 |
| `backend/tests/test_entry_router.py` | 新建 |
| `backend/tests/test_merge_node.py` | 新建 |
| `frontend/templates/ai/trace_dashboard.html` | 新建 |
| `README.md` | 修改 |
